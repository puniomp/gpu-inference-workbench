apiVersion: v1
kind: Service
metadata:
  name: gpu-inference-workbench
spec:
  type: ClusterIP
  selector:
    app: gpu-inference-workbench
  ports:
    - name: http
      port: 8000
      targetPort: 8000
